#! /usr/bin/bash

#themes="`command bat --list-themes`"; select BAT_THEME in ${themes[@]}; do export "BAT_THEME=$BAT_THEME"; echo $BAT_THEME; break; done


echo "Welcome to the magical color changer faggot" | toilet -f term --gay
echo "Type the number corresponding with the colors you would like to edit" | toilet -f term --gay

select choice in vivid bat both cancel; do 
echo "You're editing $choice"; break
done
#echo $program
#break
#done


echo "Enter the number corresponding to the Bat theme you would like to set:"
echo " "
bat_themes="$(command bat --list-themes)"
select my_bat in $bat_themes
do export BAT_THEME="$my_bat"
unset bat_themes
echo "BAT_THEME changed to $BAT_THEME"
break
done





echo "Enter number to corresponding theme you wish to try"
echo ""
ls_themes="$(vivid themes)"
select my_theme in $ls_themes
do export LS_COLORS="$(vivid generate $my_theme)"
unset theme
echo "LS_COLORS changed to: $my_theme"
break
done

#  select

 # Bash builtin construct for creating menus.
 # More information: https://www.gnu.org/software/bash/manual/bash.html#index-select.

#  - Create a menu out of individual words:
#    select word in apple orange pear banana; do echo $word; done
#
#  - Create a menu from the output of another command:
#    select line in $(command); do echo $line; done#
#
#  - Specify the prompt string for `select` and create a menu for picking a file or folder from the current directory:
#    PS3="Select a file: "; select file in *; do echo $file; done#
#
#  - Create a menu from a Bash array:
#    fruits=(apple orange pear banana); select word in ${fruits[@]}; do echo $word; done
