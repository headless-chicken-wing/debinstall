#!/usr/bin/env bash

#Display block devices and their respective mount locations if applicable


echo -e " "

lsblk --output NAME,SIZE,TYPE,FSTYPE,LABEL,FSSIZE,FSUSED,FSAVAIL,FSUSE%,PATH,MOUNTPOINTS | /usr/bin/bat -p -P -l perl

echo -e " "

exit 1
